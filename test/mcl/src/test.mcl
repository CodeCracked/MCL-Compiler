namespace test
{
    listener mcl:reload()
    {
        float a = 4.5;
        float b = 2 * a;
        int i = (int)b;
        native ''tellraw @a %{i.text_component}'';
    }

    void test_method(int i)
    {
        native "tellraw @a %{i.text_component}";
    }
}
