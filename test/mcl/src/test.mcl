namespace test
{
    listener mcl:reload()
    {
        print(mcl:pow(16, 0.5));
        print(mcl:pow(4, 2));
    }

    float add(float a, float b) { return a + b; }
    float multiply(float a, float b) { return a * b; }

    void print(float number)
    {
        int i = (int) number;
        native "tellraw @a %{i.text_component}";
    }
}
