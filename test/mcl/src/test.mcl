namespace test
{
    event test_event(int var);

    listener test_event(int var)
    {
        int double = 2 * var;
        native "tellraw @a %{double.text_component}";
    }

    listener mcl:reload()
    {
        float a = 4.5;
        float b = 2 * a;
        int i = (int)b;
        native ''tellraw @a %{i.text_component}'';
    }
}
